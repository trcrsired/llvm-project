// Generated by CMake from config.h.in; do not edit.
//
// Build-time configuration for libherbceptions, included by
// <herbceptions/error> so consumers see exactly the configuration the
// library was built with (mirrors libc++'s __config_site).
#ifndef _LIBHERBCEPTIONS_CONFIG_SITE
#define _LIBHERBCEPTIONS_CONFIG_SITE

// 1 when this is the header-only freestanding build (no compiled runtime;
// a freestanding environment provides its own domain singletons through the
// weak ABI entry points).
#cmakedefine01 _LIBHERBCEPTIONS_FREESTANDING

// Version.
#define _LIBHERBCEPTIONS_VERSION "@HERBCEPTIONS_VERSION@" 

#endif // _LIBHERBCEPTIONS_CONFIG_SITE
